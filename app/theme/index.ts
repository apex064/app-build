import {appColors} from './colors';
import {typography} from './typography';
import {appSpacings, appIconSizes, appRadius} from './others';
import {horizontalScale, moderateScale, verticalScale} from './responsive';

export {
  appColors,
  typography,
  appSpacings,
  appRadius,
  appIconSizes,
  horizontalScale,
  moderateScale,
  verticalScale,
};
